{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "lumina://standards/admin-command-schemas/assign-ta.json",
  "schema_version": "1.0.0",
  "last_updated": "2026-08-03",
  "title": "assign_ta",
  "description": "Assign an assistant/reviewer to one or more subject entities. Reviewer-role operators can assign linked assistants; domain authorities can assign any assistant.",
  "type": "object",
  "required": [
    "operation",
    "params"
  ],
  "additionalProperties": false,
  "properties": {
    "operation": {
      "const": "assign_ta"
    },
    "target": {
      "type": "string"
    },
    "params": {
      "type": "object",
      "required": [
        "ta_id",
        "student_ids"
      ],
      "additionalProperties": false,
      "properties": {
        "ta_id": {
          "type": "string",
          "minLength": 1,
          "description": "User ID or display name of the assistant/reviewer."
        },
        "student_ids": {
          "type": "string",
          "minLength": 1,
          "description": "Legacy field name for comma-separated subject user IDs or display names."
        }
      }
    }
  }
}
